<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/drawer_all_idea_list"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="@string/drawer_idea_list" />
        <item
            android:id="@+id/drawer_my_idea_list"
            android:icon="@drawable/ic_menu_gallery"
            android:title="@string/drawer_my_idea" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Partager une liste" />
            <item
                android:id="@+id/nav_backup"
                android:icon="@drawable/ic_menu_send"
                android:title="Backup Ideas (dev)" />
        </menu>
    </item>

</menu>
